/**
 * Frontfiles — Newsroom directory placeholder (NR-D3 stub for J2)
 *
 * Served from `newsroom.frontfiles.com/` via the proxy rewrite
 * in src/proxy.ts.  The real directory UI lands in NR-D13.
 */

export default function NewsroomDirectoryPlaceholder() {
  return (
    <div>
      <h1>Frontfiles Newsroom</h1>
      <p>Public press distribution for verified organisations.</p>
      <p>The live directory lands in NR-D13.</p>
    </div>
  )
}
